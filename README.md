# Pizzaria app created in java using JFrame

Requires:
1. A mysql database called `pizzaria`
2. Downloading `mysql connector` and adding it to the project
3. The username and password need to be inserted into the `Conn.java` file

Default credentials:
- Username: "root"
- Password: ""
